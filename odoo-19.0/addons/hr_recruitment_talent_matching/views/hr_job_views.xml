<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_hr_job_form_inherit_talent_matching" model="ir.ui.view">
        <field name="name">hr.job.form.inherit.talent.matching</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr.view_hr_job_form"/>
        <field name="arch" type="xml">
            
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_shortlist" 
                        type="object" 
                        class="oe_stat_button" 
                        icon="fa-users">
                    <field name="shortlist_count" widget="statinfo" string="Shortlist"/>
                </button>
            </xpath>

            <xpath expr="//group[@name='hiring_process']" position="inside">
                <field name="matching_rule_id" options="{'no_create': True}" string="Matching Rule"/>
            </xpath>

        </field>
    </record>
</odoo>