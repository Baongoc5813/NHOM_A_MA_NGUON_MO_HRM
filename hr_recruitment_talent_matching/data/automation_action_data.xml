<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="action_bulk_reconnect_talent_pool" model="ir.actions.server">
            <field name="name">Send Reconnect Emails in Bulk</field>
            <field name="model_id" ref="model_hr_talent_pool"/>
            <field name="binding_model_id" ref="model_hr_talent_pool"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">
if records:
    records.action_send_reconnect_email()
            </field>
        </record>
    </data>
</odoo>


